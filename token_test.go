package main

import (
	"testing"
)

func TestTokenValidation(t *testing.T) {
	/*
		tokenValidatorMock := mocks.NewTokenValidator(t)
		tokenValidatorMock.On("ValidateToken", "valid-token").Return(true, nil)

		app := App{
			tokenMiddleware: tokenValidatorMock,
		}

		router := setupRouter(&app)

		router.ServeHTTP(w, req)
	*/
}
